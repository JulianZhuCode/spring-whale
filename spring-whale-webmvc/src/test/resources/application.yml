spring:
  messages:
    basename: messages
    encoding: UTF-8
    cache-duration: 0s
    fallback-to-system-locale: false
  whale:
    web-mvc:
      security:
        permit-all-urls:
          - "/**"
        csrf-enabled: false
